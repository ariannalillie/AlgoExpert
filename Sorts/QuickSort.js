// Like merge sorts exploits the fact that arrays of 0 or 1
// elements are always sorted.
// Works by selecting one element (called the "pivot") an d finding
// the index where the pivot should end up in the sorted array.
